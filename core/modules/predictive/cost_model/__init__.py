# core/modules/predictive/cost_model — cost formulas, calibrated rule costs,
# and platform budget profiles. Everything numeric flows through the
# Prediction band type (prediction.py); nothing in this package emits a bare
# scalar estimate.
