# core/modules/predictive/layers — the five analysis layers of the
# Predictive Profiler. Each layer consumes raw session sections and emits
# CostItems + aggregate Predictions; layer4 turns aggregates into risk
# scores; layer5 replays a selection against a cached report.
