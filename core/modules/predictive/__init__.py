# core/modules/predictive — Predictive Profiler (Studio tier).
#
# Static-analysis performance prediction: estimates CPU/GPU frame cost,
# memory (VRAM/RAM/GC), and build (size/package/load) impact BEFORE the
# project is run or packaged. See docs/predictive/ARCHITECTURE.md.
