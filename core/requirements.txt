# llama-cpp-python publishes prebuilt CPU wheels at this index for every
# supported (python, OS, arch) combination. Without this, pip would try
# to compile from source, which on Windows requires MSVC Build Tools.
--extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cpu

fastapi==0.111.0
uvicorn==0.30.1
pydantic==2.7.1
# torch is installed separately in Dockerfile (needs PyTorch CPU index)
transformers==4.41.0
opencv-python-headless==4.9.0.80
pytest==8.2.0
httpx==0.27.0
motor==3.3.2
pymongo==4.6.1
tree-sitter==0.21.0
tree-sitter-cpp==0.21.0
# Used by shint_secure_loader for AES-256-GCM rule decryption when the
# free-tier image is run with SHINTTOOLS_AGENT_KEY set by the launcher.
cryptography==43.0.1
# Sprint C — local LLM agent (DeepSeek Coder via llama.cpp).
# Models are fetched from ShintTools' GitHub Releases via urllib (stdlib),
# NOT from Hugging Face — keeping the runtime free of third-party telemetry
# is a hard requirement for the offline / on-prem deployment.
llama-cpp-python==0.3.2
